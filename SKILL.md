---
name: douyin-data-diagnosis
description: 基于用户提供的抖音数据口播方法，诊断口播作品的观看、流量来源和账号承接。适用于需要明确数据缺口、计算相对留存并在可比样本间作出诊断时；不输出无来源的平台阈值或播放量因果承诺。
---

# 抖音口播数据诊断

## 任务

把用户提供的抖音作品数据转成可核验的诊断：用户在哪个观看阶段流失、主动选择从哪个入口发生、当前哪些结论成立、哪些数据不足以判断。

本 Skill 的唯一方法源是用户提供的《抖音平台数据理解》逐字稿。其核心不是给某个单一百分比判好坏，而是把同一批播放放回观看路径中比较。

## 适用与边界

适用：抖音口播、知识与商业类账号的单条复盘、相近作品横向比较、账号周期复盘、诊断前数据清单。

不适用：

- 只有播放量或点赞数时，断言开头、内容、封面或选题的因果；
- 跨时长作品直接比较完播率或平均观看占比；
- 没有搜索词、主页访问或咨询数据时，断言内容带来精准客户或成交；
- 以平台通用阈值替代用户自己的可比样本。

## 不变量

1. 先检查数据来源、观察窗口、投流和可比性，再解释指标。
2. 作品层面按原方法顺序评价：平均播放时长与平均播放占比 -> 2 秒跳出率与 5 秒完播率的联动 -> 封面点击率 -> 完播率。
3. 流量层面按原方法优先看：搜索 -> 个人主页 -> 推荐及其他来源。
4. 账号层面优先看主页访问；播放、互动和涨粉只作为补充，不可替代主页访问。
5. 不把 2 秒跳出率、5 秒完播率、完播率、主页来源播放或搜索流量单独解释成内容质量、精准度、信任或成交。
6. 不虚构平台机制、用户动机、阈值或缺失数据。计算中的估算必须标为估算。

## 输入模式

先只问一句：**“你要先诊断当前一条作品，还是诊断整个账号周期？”**

除非用户主动选择账号周期或商业承接，否则默认进入“当前作品基础诊断”。不得先发送字段表、要求用户手填数据或一次索取全部截图。

### 默认入口：当前作品基础诊断

直接发送 [三图一作品采集说明](references/screenshot-intake.md)。用户只需返回：

1. 作品数据概览截图；
2. 完整观众留存截图；
3. 完整流量来源截图；
4. 作品封面、标题和前 15 秒内容（作品链接、录屏或逐字稿任选其一）。

截图页面名称可能随创作中心版本变化。若找不到某一页，不让用户猜字段，要求其从该作品的数据详情页截完整页面回来再识别。

收到后先完成作品观看和来源诊断。只有当前结论确实依赖下列数据，才按需追加：

- 要诊断账号是否承接住单条兴趣：追加账号数据总览中的主页访问截图；
- 要判断搜索带来的是什么需求：追加搜索词截图；
- 要判断咨询、成交或商业价值：追加用户自己的私信、咨询和成交台账。

### 深度诊断入口

用户明确要求横向比较、账号周期诊断或商业承接诊断时，再使用 [完整字段与来源](references/input-schema.md) 和 [深度诊断采集表](references/intake-template.md)。

### 账号周期诊断入口

用户选择“诊断整个账号周期”时，不直接索取期间每一条作品的数据。先发送 [账号周期两阶段采集说明](references/account-cycle-intake.md)：

1. 先收账号数据总览与该周期作品列表两张图；
2. 根据作品列表，选出高播放、常态播放、低播放各一条代表作品；
3. 只为这三条代表作品分别补“三图一作品”材料；
4. 如果三条不足以解释异常，再说明原因并按需追加，不能无差别索取全量作品截图。

账号周期报告必须把“周期整体发生了什么”与“代表作品的表现差异”分开写。没有作品级材料时，只能诊断账号整体，不能判断具体内容或开头。

按用户任务归类：

- **数据清单**：用户想知道后续要提供什么。先给 [三图一作品采集说明](references/screenshot-intake.md)；只有对方选择深度诊断才给完整表。
- **单条诊断**：用户提供一条作品数据。检查最小字段；缺字段时只输出缺口和能成立的有限结论。
- **横向比较**：用户提供两条以上作品。先按时长、发布时间窗口、是否投流、流量来源构成建立可比组；不可比时分组，不强行排名。
- **账号周期诊断**：用户提供一段时间的作品明细、流量来源和账号数据。先形成基线，再输出分层结论。

用户需要直接提交结构化数据时，提供 [深度诊断采集表](references/intake-template.md)，要求同一观察窗口内导出或截图，不能把不同天数的数据混在同一行。

## 必算指标

令：

- `P2` = 2 秒跳出率；
- `R5` = 5 秒完播率；
- `Tavg` = 平均播放时长（秒）；
- `L` = 视频总时长（秒）。

计算：

```text
2 秒存活率 = 1 - P2
2→5 秒相对留存 = R5 / (1 - P2)
2→5 秒相对流失 = 1 - 2→5 秒相对留存
平均播放占比 = Tavg / L
总观看时长 = 播放量 × Tavg
```

仅当 `R5 <= 1 - P2` 时计算相对留存；否则报告口径、观察窗口或抄录错误，不继续推导。

原方法中出现过“2→5 秒价值传递”的两种说法。统一规则是：

- `R5 / (1 - P2)` 表示**相对留存**；
- `1 - R5 / (1 - P2)` 表示**相对流失**。

不得把两者混称为同一个指标。

### 原稿中的 5 秒后观看时长估算

原稿还提供了一个估算，而不是后台直接指标。令 `N` 为播放量，采用原稿的默认假设：2 秒前跳出者平均看 `a = 1` 秒，2 到 5 秒之间跳出者平均看 `b = 3.5` 秒。

```text
2 秒前跳出人数 = N × P2
2 到 5 秒跳出人数 = N × (1 - P2 - R5)
5 秒后存活人数 = N × R5
5 秒后人群估算总观看时长
  = N × Tavg - (N × P2 × a) - (N × (1 - P2 - R5) × b)
5 秒后人群估算平均观看时长
  = 5 秒后人群估算总观看时长 / (N × R5)
```

只有 `N`、`P2`、`R5`、`Tavg` 均已提供、人数不为负且估算结果合理时才计算。报告必须同时写明：这是沿用原稿 1 秒与 3.5 秒假设得到的**估算**，不是抖音后台直接数据，也不能用来证明平台推流原因。

## 工作流程

1. 先按 [三图一作品采集说明](references/screenshot-intake.md) 收取当前作品的材料；只列出影响当前结论的缺失截图。
2. 单条作品先做描述性诊断；只有用户给了同口径历史作品时才建立可比组。可比组优先同一账号、相近时长、相近发布时间、同为自然流或同为投流、相近流量来源构成。
3. 按 [指标解释与判断边界](references/metric-interpretation.md) 计算和解释作品数据。
   - 若用户给齐播放量、2 秒跳出、5 秒完播和平均播放时长，按 [诊断动作与建议边界](references/decision-rules.md) 给出 5 秒后人群观看时长估算及其限制。
4. 再解释搜索、个人主页、推荐和其他来源；区分“个人主页来源播放”和“账号主页访问量”。
5. 用户选择账号周期诊断时，按 [账号周期两阶段采集说明](references/account-cycle-intake.md) 先看账号总览，再解释代表作品差异；重点检查主页访问与播放、关注、互动的关系。
6. 输出 [诊断报告格式](references/report-template.md)。

## 停止条件

- 缺少最小字段时，不输出“开头好坏”“内容质量”“精准客户”等强结论；只给数据缺口、已知事实和下一次应补的截图。
- 只有一条作品时，不创建“账号标准”；可给单条描述、计算结果和后续比较要求。
- 没有同口径的可比样本或样本不可比时，只给观察，不给优劣排序与规则升级。
- 诊断到平台数据边界即停止；成交、咨询、私域承接需要用户另行提供业务记录，不能从播放数据反推。

## 输出要求

- 每个结论标注为“事实 / 计算结果 / 有条件推断 / 无法判断”。
- 指出每个结论依赖的具体数据，不把建议写成平台定律。
- 建议必须对应一个数据缺口或一个已发现的阶段问题；一次最多建议下一轮验证一个变量。
- 用户要求“考评标准”时，只能输出用户自己的同口径可比组中的中位数、分位区间或相对位置；没有可比组时，明确“暂无评分线”。不得把任意样本数量写成平台或账号的正式评分门槛。

## 验证

静态校验后，用 `evals/evals.json` 的正常样本检查：是否计算相对留存和相对流失、是否拒绝跨时长完播比较、是否列出缺失的主页访问与搜索数据。评测者不得把 `expected_behavior` 提前提供给执行者。
